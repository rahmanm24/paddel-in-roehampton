extends layout
block content
    .container
        .col-xs-12 
            h2 #{stand.standName}
            h3 #{stand.location}
            p #{stand.bikes} bikes are available at this stand

            a(href='/take-bike/' + stand.standID ) Take Bike 
            <br>
            a(href='/return-bike/' + stand.standID ) Return Bike